# Frontend environment. Copy to .env.local for overrides.
# Leave blank to use the Vite dev proxy (recommended for local dev).
# Set only if the backend runs on a non-proxied origin.
# VITE_API_BASE_URL=http://localhost:8000
